class Post {
  Post(this.id, this.createdTime, this.content, this.authorId, this.authorName, this.authorEmail);

  String id;
  String createdTime;
  String content;
  String authorId;
  String authorName;
  String authorEmail;
}